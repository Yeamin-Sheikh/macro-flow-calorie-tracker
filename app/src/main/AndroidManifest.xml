<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacroFlow">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Log Food Activity -->
        <activity
            android:name=".AddFoodActivity"
            android:label="@string/title_add_food"
            android:parentActivityName=".MainActivity" />

        <!-- Daily Historical Log Activity -->
        <activity
            android:name=".DailyLogActivity"
            android:label="@string/title_daily_log"
            android:parentActivityName=".MainActivity" />

        <!-- Profile, Goals & TDEE Calculator Activity -->
        <activity
            android:name=".ProfileGoalsActivity"
            android:label="@string/title_profile_goals"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
